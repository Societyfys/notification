post:
  tags :
    - sentNotification
  operationId: sentNotification
  summary : sent notification

  description : sent notification

  parameters :
    - $ref: '../common/common-request-headers.yaml#/Accept'
    - $ref: '../common/common-request-headers.yaml#/CorrelationId'

  requestBody:
    description: sent notification request
    required: true
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/SentNotificationRequest'

  responses:
    '200':
      description: Successfully
      headers :
        correlationId:
          schema :
            type: string
          description: "Helps in getting the flow of the request when we log the correlation id"


    '400':
      description: Invalid Input
      headers :
        CorrelationId:
          schema :
            type: string
          description: "Helps in getting the flow of the request when we log the correlation id"
      content :
        application/json:
          schema :
            $ref: '../common/error-response.yaml'

    '404':
      description: Resource not Found
      headers :
        CorrelationId:
          schema :
            type: string
          description: "Helps in getting the flow of the request when ve log the correlation id"
      content :
        application/json:
          schema :
            $ref: '../common/error-response.yaml'


    '500':
      description: Internal Server Error
      headers :
        CorrelationId:
          schema :
            type: string
          description: "Helps in getting the flow of the request when ve log the correlation id"
      content :
        application/json:
          schema :
            $ref: '../common/error-response.yaml'

    '503':
      description: Service request currently not available
      headers :
        CorrelationId:
          schema :
            type: string
          description: "Helps in getting the flow of the request when ve log the correlation id"
      content :
        application/json:
          schema :
            $ref: '../common/error-response.yaml'

components:
  schemas :
    SentNotificationRequest:
      title: SentNotificationRequest
      type: object
      properties:
        userId:
          type: string
        notificationId:
          type: string
        variables:
          type: object
          additionalProperties:
            type: object


#    Variable:
#      title: Variable
#      type: object
#      properties:
#        name:
#          type: string
#        value:
#          type : object


